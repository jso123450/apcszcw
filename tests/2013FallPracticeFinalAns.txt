1) A
2) B
3) D
4) E
5) 
6) E
7) A
8) D (maybe A?)
9) C
10) B
// need to fix
11) public int nextTankToFill(int threshokd){
    	   int output = filler.getCurrentIndex;
	   for (int i = 0; i < tanks.size(); i++){
	       int possibleOutput = output
	       int test = tanks.get(i).getFuelLevel();
	       if (test <= threshold && test < tanks.get(possibleOutput).getFuelLevel())
	       	  possibleOutput = i;
	   }
